# talent-lab - AI 赋能的智能简历分析平台

## 背景
在招聘流程中，快速筛选和分析大量简历是一项耗时的工作。
本全栈应用能够上传和解析简历（PDF 格式），利用 AI 模型提取关键信息并进行智能评分，同时提供一个 交互丰富、体验优秀 的前端界面来管理和可视化分析候选人数据。

## 技术栈
- 前端框架：TypeScript + React
- 后端语言：Python + Flask
- 数据存储：SQLite + SQLAlchemy
- API 规范：RESTful

## 模块

### 模块一：简历上传与解析
- 提供文件上传组件，支持 **拖拽上传** 和 **点击上传**，限制 PDF 格式
- 支持 **批量上传**（至少同时上传 5 份简历），展示上传进度和状态
- 后端解析 **PDF** 内容，提取文本信息（需兼容多页简历）
- 对提取文本进行清洗和结构化处理
- **加分项：** 上传过程中展示实时预览（PDF 首页缩略图）

### 模块二：AI 智能信息提取
利用 AI 模型（如 OpenAI API）从简历文本中提取以下结构化信息：

| 类别 | 字段 |
| --- | ----------- |
| 基本信息 | 姓名、电话、邮箱、所在城市 |
| 教育背景 | 学校、专业、学历、毕业时间 |
| 工作经历 | 公司名称、职位、时间段、工作内容摘要 |
| 技能标签 | 技术栈、工具、语言等（以标签形式提取） |
| 项目经历 | 项目名称、技术栈、个人职责、项目亮点 |

- AI 提取过程通过 **SSE（Server-Sent Events）** 流式返回，前端逐步渲染提取结果
- 支持用户在前端手动修正 AI 提取的信息

### 模块三：职位机会、申请追踪与智能匹配
- 面向个人求职者保存职位机会：来源、JD 原文、结构化职责与要求、薪酬、联系人和个人判断
- 粘贴 JD 后提取公司、职位、地点、经验、学历、技能和薪资；解析结果确认后再应用
- 每个职位可匹配多份简历，并记录实际投递的简历版本
- 保存当前申请状态和完整时间线，支持补录笔试、面试、Offer、备注和待办
- 调用 AI 模型对简历与职位要求进行匹配分析，返回：
  - **综合匹配度评分** （0-100 分）
  - **各维度子评分：** 技能匹配度、经验相关性、教育背景契合度
  - **AI 评语：** 一段文字说明该候选人的优势与不足
- 评分结果以 **可视化图表** 展示（如雷达图 / 柱状图 / 环形进度条）
- 支持多个 JD 同时对比评分

### 模块四：候选人管理面板
- 实现候选人列表页，支持： 
  - **表格视图 / 卡片视图** 切换
  - 按评分、上传时间、技能标签 **排序与筛选**
  - 关键字搜索（姓名、技能、学校等）
  - 分页或虚拟滚动
- 点击候选人进入 **详情页**，展示完整的解析信息、评分详情、原始 PDF 预览
- 支持候选人状态管理（待筛选 → 初筛通过 → 面试中 → 已录用 / 已淘汰），状态变更需有视觉反馈
- **加分项：** 支持候选人对比功能（选择 2-3 人，并排对比各维度评分）

### 模块五：前端工程质量
- 使用 **组件化架构** ，合理拆分组件粒度，体现复用性和可维护性
- 页面整体需具备 **良好的视觉设计和交互体验**（不接受纯原生 HTML 样式）
- 实现 **响应式布局**，适配桌面端（≥1280px）
- 全局错误处理与 Loading 状态管理
- 加分项： 
  - 暗色 / 亮色主题切换
  - 骨架屏（Skeleton）加载态
  - 键盘快捷键支持
  - 动画过渡效果

## 考量维度

| 维度 | 权重 | 说明 |
| --- | ----------- |
| 前端工程能力 | 30% | 组件设计与抽象能力、TypeScript 类型安全、状态管理方案、自定义 Hook 的合理使用 |
| 交互体验与视觉 | 20% | 上传交互、数据可视化、列表交互的流畅度与细节打磨、视觉一致性 |
| 全栈整合能力 | 20% | 前后端联调、API 设计合理性、SSE 流式通信、数据库设计 |
| 代码质量与工程化 | 20% | 代码结构清晰、Git 提交规范（Conventional Commits）、README 完整性、Lint 配置、错误处理 |
| 加分项与创新 | 10% | 可选功能的实现深度、技术选型的合理性、创新性方案 |
